# 🐛 BUG REGISTRY & REPEAT PREVENTION

| Bug ID | Title & Symptom | Root Cause | Affected File(s) | Fix & Release | Do Not Repeat |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **BUG-2026-08-08-001** | `Unknown column '0.0'` in Query Builder | CI4 Query Builder auto-escaped literal `0.0 as distance` with backticks | `app/Services/BaselineService.php` | Pass `false` 2nd arg to `$builder->select(..., false)` (`v2.3.0.38`) | Do NOT pass literals into `select()` without `false` |
| **BUG-2026-08-08-002** | HTTP 403 Forbidden on 2nd Guided Point | CSRF token invalidated on 1st AJAX submit; 2nd submit used stale token | `app/Controllers/InspectionController.php`, `app/Views/inspections/guided.php` | Return `csrf_token` & `csrf_hash` in JSON response and update JS header (`v2.3.0.38`) | Do NOT submit sequential AJAX POSTs without dynamic CSRF token refresh |
| **BUG-2026-08-08-003** | `Undefined array key "judul_pekerjaan"` on Asset Detail | DB column is `judul_wo`, but View assumed `judul_pekerjaan` | `app/Views/assets/detail.php` | Safe defensive coalescing `<?= esc($wo['judul_wo'] ?? $wo['judul_pekerjaan'] ?? '-') ?>` (`v2.3.0.39`) | Do NOT assume physical DB column is `judul_pekerjaan` |
| **BUG-2026-08-08-004** | `Unknown column 's.kode_section'` on Asset Health Index | Physical `sections` table has no `kode_section` column | `app/Services/AssetHealthService.php` | Select `NULL as kode_section` using `$builderSec->select(..., false)` (`v2.3.0.40`) | Do NOT select `s.kode_section` from `sections` table |
| **BUG-2026-08-08-005** | `Undefined array key "badge_class"` on Smart WO | `SmartWorkOrderService` looped over `$woList` instead of `$optimizedWos` before array_slice | `app/Services/SmartWorkOrderService.php`, `app/Views/smart_wo/index.php` | Enrich `$optimizedWos` directly in producer and add safe fallback in view (`v2.3.0.41`) | Ensure array enrichment modifies the exact returned array variable |
| **BUG-2026-08-08-006** | `Undefined array key "petugas_assigned"` on Smart WO | View used unsafe ternary `?:` which fails on missing array keys | `app/Views/smart_wo/index.php` | Replaced all `?:` with null coalescing `??` across all array keys (`v2.3.0.42`) | Do NOT use `?:` for array key access in PHP views |
| **BUG-2026-08-09-001** | Soft-deleted assets leaking into Progress/Baseline/Guided | Baseline & Guided queries lacked `deleted_at IS NULL` predicate | `app/Services/BaselineService.php`, `app/Controllers/InspectionController.php`, `app/Controllers/InspectionProgressController.php`, `app/Views/inspections/guided.php` | Added `deleted_at IS NULL` to all 3 SQL queries and added JS guard clause in `guided.php` (`vA-asset-integrity`) | Always include `deleted_at IS NULL` on asset joins |
| **BUG-2026-08-09-002** | `Uncaught SyntaxError: Unexpected end of input` at gis:1869 | `btn-locate-me` event listener callback missing closing `});` | `app/Views/gis/index.php` | Fixed JS syntax closing brackets (`vB.1-js-syntax-fix`) | Always check JS syntax with `php -l` and ensure callbacks close |
| **BUG-2026-08-09-003** | `Uncaught TypeError: L.markerClusterGroup is not a function` | MarkerCluster CDN unpkg URL had CORS/latency issues | `app/Views/gis/index.php` | Updated CDN URL to cdnjs and added defensive capability detection `typeof L.markerClusterGroup === 'function'` (`vB.2-markercluster-resolution`) | Add defensive capability detection for Leaflet plugins |
