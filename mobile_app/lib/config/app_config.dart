class AppConfig {
  // GANTI link di bawah ini dengan URL server hosting Anda jika sudah siap
  // Contoh: 'https://domain-anda.com/public/api'
  static const String apiBaseUrl = 'http://localhost/SIDAK%20TEJO/public/api';

  // Timeout koneksi API (dalam detik)
  static const int apiTimeout = 15;
}
